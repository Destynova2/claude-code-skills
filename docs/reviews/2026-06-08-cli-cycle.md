# cli-cycle Review — cli-code-skills

Date: 2026-06-08
Scope: repository root

## Result

The cycle findings were resolved in this pass:

- skill inventory is now generated against 30 installable `SKILL.md` files;
- README and CLAUDE.md document Claude Code and Codex installation;
- `shared/` and `gotchas.md` are included in install/update commands;
- root `SKILL.md` files use `../shared` and `../gotchas.md`;
- `cli-cycle` resolves `SKILLS_ROOT` instead of hardcoding `~/.claude/skills`;
- repository validation is automated with `scripts/validate_skills.py`;
- GitHub Actions runs the validator on pushes and pull requests.

## Verification

```bash
python3 scripts/validate_skills.py
git diff --check
```

Expected result:

```text
OK: 30 skills validated
```
